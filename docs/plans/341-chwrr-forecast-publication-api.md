---
status: DRAFT
created: 2026-09-24
revised: 2026-09-26
plan: 341
title: CHWRR forecast review and publication API
depends_on: [340]
blocks: [342, 343, 344]
related: [042, 147, 266, 273, 328, 401, 402]
reviews:
  - "2026-09-26 independent gpt-6-astra full-plan review — no remaining findings"
  - "2026-09-26 independent gpt-6-sol full-plan review — no remaining findings"
  - "2026-09-26 additional independent gpt-5.6-terra high-risk review — no remaining findings"
open_decisions:
  - The CHWRR identity provider is not yet selected. The owner accepted an OIDC contract tested against a test provider; confirm the real issuer, API audience, signing keys/algorithm, subject claims, and dashboard origin before enabling CHWRR writes. Revisit this plan if the chosen provider cannot supply this contract.
  - Confirm that the chosen identity provider supplies an MFA assurance claim (or an equivalent verifiable login policy) meeting `docs/standards/security.md`; a valid signed token alone does not establish MFA.
source: 2026-09-24 owner — a selected CHWRR hydrologist must explicitly publish each chosen forecast for authenticated API use, with historical publication and audited withdrawal. 2026-09-25 owner — publishing a newer approved forecast leaves the older publication in history; routine replacement does not require withdrawal. A captured forecast may be published while its own protected backup proof is pending if the backup system is healthy; an overdue proof needs an operations alert.
---

# Plan 341 — CHWRR forecast review and publication API

## Status and scope

**DRAFT — not implementable.** Security, human authorization, database migration, and external API behavior make this high-risk under `docs/workflow.md`. Independent review found no remaining plan findings; only the orchestrator may later set READY.

This is backend support for the separately built `sapphire-flow-map` dashboard. It follows Plan 340's first-run evidence capture and covers forecast review, one-forecast publication, withdrawal, and authenticated consumer reads. It does not build the dashboard, bulletin artifacts, warning publication (Plan 342), CAP (Plan 266), email/Slack delivery (Plan 110), or model editing. `docs/v0-scope.md` intentionally deferred Flow 3; this is a CHWRR follow-on, not a claim that v0 already shipped review.

## Existing boundary and decisions

- `ForecastStatus` has `raw | reviewed | published | superseded`. Plan 328's automatic retry may change an existing row to `superseded` and insert a new RAW forecast for the same natural key; it retains the old values and evidence. `PgForecastStore.transition_status` changes forecast status in place with optimistic versioning but has no API caller or publication audit event. The existing status column cannot be the authority for CHWRR publication: an automatic retry could otherwise silently remove a human-approved forecast from consumer reads. `audit_log` and `AuditEventType.FORECAST_STATUS_CHANGE` exist (`types/enums.py`, `store/forecast_store.py`, `db/metadata.py`).
- Current authenticated `GET /api/v1/forecasts/{id}`, station forecast list, and forecast-lab snapshot can expose raw forecasts (`api/routes/api_forecasts.py`, `api_stations.py`, `forecast_lab.py`). A publication gate must cover all consumer-facing forecast paths, including legacy JSON and caches/exports.
- Service access tokens are GET-only consumer/admin identities today (`api/security.py`, `standards/security.md`); Plan 401 proposes an additional GET-only, tenant- and station-scoped reviewer token for each dashboard. No service token identifies the hydrologist who approves publication or receives a write grant. A verified CHWRR human identity and local station grants authorize unpublished-candidate review and publication.
- The architecture's `users`/password/TOTP model is a v1.x design, not an implemented human-user store or login flow (`docs/standards/security.md`). Reuse its local user identity and audit concept: one local user UUID linked to external `(issuer, sub)`, active/tenant status and station grants. Use OIDC for authentication; do not create a parallel hydrologist identity silo or build local password/TOTP login for this MVP. If the future human-session stack is built, it must reuse these user IDs and grants or migrate them explicitly.
- Activate published-only reads per CHWRR tenant after the review/write path, Plan 342's warning recheck and Plan 340's protected evidence backup/restore are ready. Preserve existing Swiss/internal diagnostic access to RAW forecasts in a separately authorized internal route; do not change every service-token route globally while this tenant-specific workflow is being introduced. Explicitly classify each existing API/legacy JSON/forecast-lab/export route as consumer or internal diagnostic and test the applicable principals. Plan 402's `GET /api/v1/qc/rules` and `GET /api/v1/stations/{id}/skill` are REVIEW-class internal diagnostics with no forecast values, so the publication gate does not filter their responses. Plan 401's reviewer token may read those diagnostics, but it sees only published values on ordinary forecast routes and cannot read this plan's unpublished candidates. The dashboard uses the named hydrologist's OIDC principal for candidate review and decisions.
- Rollback after activation is a security operation as well as a schema operation: a pre-gate API image would expose RAW forecasts through its old reads. The deployment runbook must block CHWRR consumer access at the proxy or keep the new gate-capable image in service while restoring data. Do not redeploy a pre-gate image with CHWRR consumer traffic enabled. Prove the rollback path in staging.
- **One click selects one immutable forecast ID for publication.** Keep CHWRR publication in an additive, append-only decision ledger and a transactionally updated current-selection projection, separate from `forecasts.status`. Do not add `ForecastStatus.WITHDRAWN` or use the forecast status column as the publication flag. `REVIEWED` remains reserved for later multi-step review. At most one forecast is currently selected for each `(tenant, station_id, parameter, issued_at)`, independent of model. The projection has a persistent row and monotonically increasing version for each key: withdrawal clears `selected_forecast_id` but does not delete the row or reset its version. A null expected version means the key has *never existed*, not merely that it is currently empty. Enforce in the database that any selected forecast ID belongs to that exact tenant/station/parameter/issue time; a unique key alone cannot prevent a mismatched or cross-tenant pointer. The decision ledger retains every previously published ID. A rejected/unselected forecast has no publication decision, whatever its generation status. The dashboard derives publication badges from this ledger, not from `ForecastStatus`.
- **Automatic retry never publishes or retracts.** Plan 328 may mark a forecast `superseded` and create a replacement RAW row. If the old ID was selected for publication, it stays selected and readable with its original values until an authorized hydrologist publishes a replacement or explicitly withdraws it. The new row is an unpublished candidate. Consumer authorization and `latest-published` queries use the publication selection, not the current-forecast filter that excludes `superseded` rows. This preserves the last human-approved answer while the replacement awaits review.
- **Evidence at the publish boundary.** Never publish a pre-0057 forecast with no Plan 340 evidence row: its values are outside the evidence-linked no-cleanup guard. Require the captured manifest, input snapshot, output values and references needed to diagnose that exact forecast, with no missing-material cause other than the pending protected-backup/image-byte proof. Review list/detail shows immutable `capture_status`, derived `effective_preservation_status`, attestation ID, remaining reasons and whether this forecast's backup proof is pending. A hydrologist may publish while *that forecast's* next protected restore proof is pending, as the owner decided for alert-day timeliness, only if Plan 340's protected target, no-cleanup policy and fresh successful backup/restore health gate are active. Store `preservation_at_publish = verified | backup_pending` on the immutable publication decision and show it in the response; schedule/reconcile the forecast-specific proof and raise an operations health alert if it is still missing after the configured freshness window (initially 36 hours). An overdue pending proof blocks further CHWRR publish writes until resolved but never silently withdraws an earlier human publication. Missing evidence, another unresolved capture gap, a stale/unhealthy protected backup or an unconfigured target blocks new publication or replacement. An authorized, reasoned withdrawal of a current or historical forecast remains possible during a backup outage, including a joint linked-warning withdrawal under Plan 342. The API must read a trusted, freshness-bounded backup-health projection written by the host backup process with restricted privileges; it cannot inspect the host-only volume or accept a caller-supplied health flag. The Mac mini has no separate protected volume, so real publication remains disabled there.
- **Routine replacement is a new publish decision, not a withdrawal.** A hydrologist may publish a newer issue time or replace the selected forecast for the *same* station/parameter/issue time. For the same key, one transaction appends the new publication decision, moves the current-selection pointer, records the replaced publication decision and forecast IDs plus actor/time, and emits a change-feed event carrying both IDs. The older publication and its values remain available in historical reads and audit history, with a derived `replaced` label; no withdrawal reason is requested. For a newer issue time, the earlier issue remains selected for its own key and historical queries; `latest-published` moves by issue time. A previously replaced forecast may be selected again only through a new attributable publish decision if it is still an eligible, non-`superseded` candidate; history is keyed by immutable decision ID so an A→B→A sequence remains unambiguous. A withdrawn forecast ID cannot be re-published. After authenticating and checking the caller's *current* station grant, an exact idempotency-key replay scoped to that actor/tenant/route returns its committed decision before current candidate/selection checks, even if a later retry superseded the forecast or another publication replaced it; reusing the key with different input remains `409`.
- Publishing a forecast is a new threshold-check input. Emit a durable, idempotent publication or replacement event in the same transaction as the decision, containing the selected forecast ID, immutable output/version, actor and replaced ID when applicable. Plan 342 consumes it to evaluate the *selected* forecast under `threshold_check_mode=published|both`, even if the raw candidate came from a different model or pooled strategy. The CHWRR consumer publication mode must not be activated until that recheck and its human warning review path work; `raw` remains an internal early-warning path. Withdrawal emits a separate event for warning reconciliation. Document the intentional one-click selection simplification against Flow 3 in `docs/architecture-context.md`.
- **Withdrawal means reasoned retraction, not routine replacement.** An authorized hydrologist may retract a currently selected *or historical replaced* forecast ID found erroneous. It requires a reason and preserves the forecast and all publication decisions. If that ID is the current selection, withdrawal clears the pointer and increments its version; if it was already replaced, the newer current selection stays intact. Withdrawal tombstones every consumer-visible publication of that forecast ID and emits an invalidation event, while an authorized CHWRR audit view retains the full record. A later different forecast for the same key may be published. No in-place correction or re-publication of the withdrawn ID.
- Do not permit withdrawal **or same-key replacement** of a selected forecast with a current linked warning until Plan 342 supplies a joint forecast/warning decision transaction. A later outbox event cannot enforce this invariant. Before that transaction exists, reject either action without changing the selection or warning. After it exists, require an explicit typed `linked_warning_withdrawal` action naming the current warning publication ID, expected warning version and reason; the dashboard explains that the old warning will be withdrawn and obtains the hydrologist's consent. One authorized action must commit the forecast publication change, warning withdrawal and both audit records together or none; a missing/stale warning action returns `409`. A historical replaced ID is not linked to a current warning under Plan 342's active-selection invariant; detect and reject any inconsistent legacy link rather than silently tombstoning it. Publishing a newer issue time does not silently retract the earlier issue's linked warning; Plan 342 decides its separate validity/replacement lifecycle.
- Consumer endpoints remain authenticated. `latest-published` returns the selected forecast from the newest published issue time per station/parameter, with a deterministic tie breaker. The ordinary consumer list returns one current selection per issue time. Historical list/detail retains *all* publication decisions, including replaced and `superseded` forecast rows with full original values; if an ID was later withdrawn, every occurrence becomes a metadata tombstone. An authorized CHWRR audit view includes full withdrawn records and their decision history. No public unauthenticated path in this slice.

## API contract for the dashboard and consumers

| Principal | Route | Behavior |
|---|---|---|
| Verified CHWRR human with a local station review grant | `GET /api/v1/review/forecasts?station_id=...&start=...&end=...` and `GET /api/v1/review/forecasts/{id}` | Scoped candidates with model, quality, forecast version, generation status, separate publication state and selection version, plus Plan 340 capture/preservation status, attestation ID, remaining reasons and backup-pending state; pagination and bounded date range. Consumer, reviewer and admin service tokens are denied. |
| Same, with publish permission on station | `POST /api/v1/review/forecasts/{id}/publish` | Body contains `expected_forecast_version`, `expected_selection_version` (null only when the key has never existed), and an idempotency key. Lock and recheck the candidate forecast inside the publication transaction against Plan 328's supersession writer; a stale or `superseded` candidate, changed selection, or stale first-publish request returns `409`. Exact-key replay returns the original result before those checks. A same-key replacement names the former selected ID and needs no *forecast* withdrawal reason; if a current warning is linked, additionally require `linked_warning_withdrawal: {action: "withdraw", warning_publication_id, expected_warning_version, reason}` with explicit hydrologist consent, or reject. |
| Same, with publish permission on station | `POST /api/v1/review/forecasts/{id}/withdraw` | The ID may be the current selection or a historical replaced publication. Body contains `expected_selection_version`, idempotency key, and required forecast-retraction reason. If the selected ID has a current linked warning, additionally require the typed `linked_warning_withdrawal` action above after Plan 342's joint transaction is active; otherwise reject. Only withdrawing the selected ID clears the pointer. Automatic forecast-version changes do not themselves authorize or prevent withdrawal. |
| Existing service consumer or admin token; after Plan 401, reviewer token | `GET /api/v1/stations/{id}/forecasts`, `GET /api/v1/forecasts/{id}`, `GET /api/v1/stations/{id}/forecasts/latest-published` | CHWRR publication gate applies to all three roles; consumer and reviewer station scope is identical, while admin retains its existing unscoped read role per `docs/standards/security.md`. List/latest use current publication selections, even if their source forecast row is `superseded`. By-ID detail serves both current and replaced published IDs with original values; an unpublished ID returns 404 and a withdrawn ID returns 410 with metadata-only tombstone. `GET` remains read-only. |
| Same read principals | `GET /api/v1/stations/{id}/forecast-publications?cursor=...&limit=...` | Bounded historical publication decisions keyed by decision ID, including current and replaced IDs with their original values and withdrawn metadata tombstones, independent of generation status. Reviewer and same-scope consumer get identical CHWRR values. |
| Same read principals | `GET /api/v1/forecast-publications?cursor=...&limit=...` | Bounded, station-scoped publication, replacement and withdrawal change feed for downstream cache invalidation; replacement names the former and new selected IDs, without exposing unpublished forecast values. Reviewer and same-scope consumer get identical CHWRR events. Return a stable next cursor. |

Response schema exposes stable forecast and publication-decision IDs, station, parameter, issue/valid times, model/artifact identity, QC/input-quality fields, `forecast_status`, `forecast_version`, `publication_state` (`unpublished | selected | replaced | withdrawn`), `selection_version`, and publication timestamp. Review responses also expose Plan 340 `capture_status`, `effective_preservation_status`, attestation ID, remaining reasons and the typed `preservation_at_publish` state (`verified | backup_pending` for a published decision); detail includes actor/time/reason decision history. `replaced` is historical publication, not withdrawal; its values remain available until reasoned retraction. If one eligible forecast ID is selected again after replacement, by-ID detail shows its latest decision while history preserves each decision ID separately. Preserve pagination totals after publication and tenant/station filtering. Choose non-conflicting route ordering before implementation. The change feed must use a transactionally allocated, commit-ordered publication sequence as its cursor rather than timestamp alone; serialized sequence allocation makes a later-committing event sort after already visible events. Return an opaque next cursor and bounded pages in sequence order, including replacement links and withdrawal tombstones. A client that retries a page or resumes after a same-time concurrent publication must neither lose nor invent an event. Document exact JSON in `docs/spec/types-and-protocols.md` and generated OpenAPI during implementation.

The proposed integration contract is CHWRR OIDC access tokens; the actual provider is not yet known. Implement against a test provider, then validate the real provider's capabilities before CHWRR activation. Validate configured issuer, audience, signature/JWKS, allowed algorithm, `iat`, `exp`, `nbf`, stable `sub`, and a trustworthy MFA assurance claim or equivalent enforced IdP policy; reject missing/malformed claims and cross-tenant or unlisted users. Enforce a maximum 30-minute human access-token lifetime (`exp - iat`) and bounded clock skew, matching `docs/standards/security.md`'s human-token exposure window; a real IdP that cannot supply this contract needs an explicit reviewed security-policy change before activation. Link `(issuer, sub)` to one local user UUID, active status, tenant, and station-scoped `review`/`publish` permissions; an operator-managed grant/revoke command is enough for MVP. Use an additive local user/external-identity schema compatible with the planned `users` model, not a second independent hydrologist account table. Do not derive permission solely from a client-provided name or group label. Capture human UUID in `audit_log.actor_id` (`actor_type=user`), and never log token bytes. IdP endpoint, claims, JWKS rollover, MFA assurance mechanism, and dashboard origin are deployment inputs, not assumed values. No service token, including Plan 401's reviewer token held server-side by a dashboard, may call a review-candidate or write route; those requests must carry the named human's verified principal. Extend the current GET-only CORS policy with only the required review POST routes, headers and exact dashboard origin; do not use wildcard credentialed origins.

## MVP dashboard handoff (separate repository)

The other dashboard implementer can use this proposed contract to demo a station/parameter/issue-time forecast row with model, issue/valid times, QC/input-quality indicator, separate generation status and `Unpublished | Published | Replaced | Withdrawn` publication badge. Show Plan 340 capture/preservation status and a clear **Backup pending** marker when the forecast's own restore proof is still due; disable Publish when evidence is absent or the protected-backup gate is unhealthy. A hydrologist with the station's publish grant can tick/select exactly one row and click **Publish**; the dashboard submits that forecast ID plus the displayed forecast and selection versions, then refreshes from the API response. Show an automatic retry's new RAW forecast as unpublished while the earlier selected ID remains published, even if its generation status is `superseded`. When the hydrologist publishes the new ID, show it as the selection and the older one as historical `Replaced`, still readable with its original values; do not prompt for a withdrawal reason. Show an authorized withdrawal action for exceptional retraction of a selected or historical publication: it asks for a reason and produces a tombstone for that ID in consumer history, leaving any different current selection in place. A read-only hydrologist can inspect but sees no enabled write control. The backend, not the UI, enforces all permissions and the one-selected-forecast constraint. Until these DRAFT plans are implemented, a demo must label local/mock responses as illustrative rather than implying the live backend already supports publication.

## Tasks

### T1 — Human principal and scoped authorization

**Outcome.** A CHWRR access token resolves to an attributable, locally authorized human principal; write permission is per station and revocable.

**In / Out.** In: `api/security.py` or separate human dependency, typed auth values, local user/external-identity/scope schema and store compatible with the planned `users` model, operator grant/revoke CLI, API configuration and exact-origin CORS, `docker/bootstrap-roles.sql`, `docs/standards/security.md`. The role bootstrap's blanket table SELECT must be followed by explicit, idempotent `sapphire_worker` SELECT revocation on every new human user, external-identity and grant table; preserve only the API role's required access. Verify the real IdP's MFA guarantee before activation; a test IdP is for contract testing only. This human write path does not depend on Plan 401's separate reviewer-token role. Out: password handling, SAPPHIRE-issued human sessions, service-token write grants, dashboard UI.

**Verification.** `uv run pytest tests/unit/api/test_human_auth.py tests/integration/store/test_hydrologist_identity_store.py`; role-grant integration test for `sapphire_api` and explicit `sapphire_worker` SELECT denial on every new human identity/grant table after initial and repeated bootstrap; verify allowed/revoked, expired, wrong audience/issuer/signature, missing/old `iat`, signed token valid for more than 30 minutes, excessive clock skew, absent or inadequate MFA assurance, missing claim, cross-station, and disallowed CORS-origin cases.

**Pre-change.** RED: no human principal/permission store exists and existing service tokens cannot identify an individual reviewer.

### T2 — Atomic forecast decision and durable audit

**Outcome.** A publish, same-key replacement or withdrawal commits one attributable append-only decision, current-selection change when applicable, audit record and change-feed event in one transaction; automatic forecast retry does not change publication. Concurrent publisher and retry behavior is deterministic.

**In / Out.** In: additive append-only publication decisions and events, a mutable persistent selection projection with a database-enforced `(tenant, station_id, parameter, issued_at)` key and monotonically increasing independent version, and a store API that serializes decisions under that key. A composite FK or equivalent DB-enforced invariant must bind `selected_forecast_id` to the same tenant/station/parameter/issue time; application lookup alone is insufficient. Publishing locks and rechecks the candidate forecast row inside the decision transaction so Plan 328 supersession and publication serialize: retry first yields a `409` stale candidate; publication first commits a selection that retry may later supersede without changing publication. For new publication or replacement, check Plan 340 evidence linkage and a trusted backup-health projection inside the transaction; the host backup process writes its verified backup ID, restore time, target separation/retention readiness and check time through a dedicated least-privilege role. The API may read but cannot write this projection; ordinary forecast workers and client tokens cannot set it. Missing/stale/unhealthy state fails closed for new publication or replacement, while authorized reasoned withdrawal, including joint linked-warning withdrawal, remains available. Permit only the owner-approved per-forecast pending-backup case and record the typed `preservation_at_publish` value on the decision. Publishing inserts a decision and atomically creates or moves the selection; replacement records the former selected ID and retains its historical publication and values. Withdrawal records a reason, tombstones every publication of that forecast ID, and clears the selection only if that ID is selected. Keep an empty selection row/version after withdrawal. Authorize writes by station and verified actor; use `audit_log`, least-privilege API grants, typed decision/reason codes, and a transactionally allocated commit-ordered change-feed sequence. Process exact actor/tenant/route-scoped idempotency-key replay after current authorization but before live eligibility/version checks, returning the committed result; changed payload remains a conflict. Do not add `ForecastStatus.WITHDRAWN` or make `forecasts.status` the publication authority. Preserve one-version schema compatibility and demonstrate rollback per `docs/standards/cicd.md`. Out: deleting forecasts, changing forecast values or model assignments, a general review workflow engine.

**QC eligibility.** Reject a forecast with `qc_status = qc_failed` inside the locked publish/replacement transaction, even when its generation status, evidence and backup health otherwise pass. Plan 404 may retain such rows for diagnosis; its separate decision about who can view them does not make them publishable.

**Rejected-record visibility.** Plan 404 D2 retains rejected member/group outputs only in the separate `rejected_forecasts` record, never in `forecasts`. Its REVIEW route carries forecast values and flag `detail`, unlike Plan 402's two metadata-only REVIEW routes. When this plan's tenant publication gate activates, wire Plan 404 D4's single `publication_gate_active(tenant_id)` predicate to the same switch: reviewer service tokens then see rule metadata without values or flag `detail`, while admin tokens retain full diagnostic access. A named CHWRR human with a current station `review` grant may inspect the full rejected record for diagnosis but cannot publish its ID; consumer tokens remain denied. Apply the rule whether Plan 404 or this plan lands first.

**Verification.** `uv run pytest tests/integration/store/test_forecast_publication_store.py tests/integration/db/test_migration_forecast_publication.py`; prove atomic rollback when selection, audit or event insert fails; concurrent same-key publishers yield one selection; stale forecast or selection version returns `409`; a null expected version cannot replay a first-publish request after publish→withdraw because the empty projection row keeps its higher version. Verify a deliberately mismatched forecast key and cross-tenant ID are rejected by the DB. Race Plan 328 retry against publication in both orders, with a locked candidate row and post-lock status/version check. Cover an automatic retry that marks a *published* source forecast `superseded` without changing its selection or exposing the new RAW forecast; human publication of the new ID at the same issue time moves the selection, retains the older full publication in history, emits a linked replacement event and requires no forecast-withdrawal reason. A newer issue time leaves the older issue's selection in history. Verify A→B→A only through a new audited decision while A remains eligible, with distinct decision IDs; an auto-`superseded` A cannot be reselected. Exact idempotency-key replay returns the original result after later supersession or replacement, while reuse with different input returns `409`; a revoked grant or another actor's use of the same key cannot retrieve that result. Verify a reasoned historical withdrawal tombstones A without changing B's current selection, selected-ID withdrawal clears the pointer, and neither permits re-publishing the withdrawn ID. In an A→B→A→withdraw A sequence, the withdrawal feed invalidates *both* A decision IDs (or declares a forecast-ID-wide invalidation), so a cache cannot retain A's earlier published values. Reject pre-0057/no-evidence candidates and missing capture material; show complete versus backup-pending preservation, record `backup_pending`, alert operations and stop new publications on overdue proof, and block when the global protected backup is unhealthy. Test that API and ordinary worker roles cannot forge the host-only healthy projection, while the dedicated host role cannot write publication decisions; prove legacy values cannot be published then pruned, and a previous image can read the additive schema. Check commit-ordered sequence with equal timestamps and concurrent transactions. Until Plan 342's joint transaction is active, fail closed on withdrawal or same-key replacement of a selection with a current linked warning, with no change to either record. Plan 342 T2 owns the post-activation joint-transaction tests.

Also test that stale/unhealthy backup health blocks new publication and same-key replacement but permits authorized, reasoned withdrawal of a selected or historical forecast when no current warning is linked. Plan 342 T2 tests linked-warning withdrawal during a backup outage.

Test a stored `qc_failed` forecast with valid evidence and healthy backup, using the already stored combined-forecast path from Plan 253 or a seeded store row; both first publish and same-key replacement must reject it without changing the selection, decision ledger, audit history or event feed. If Plan 404 has landed, assert its separate `rejected_forecasts` IDs cannot enter the forecast publish route or selection: Plan 404 D2 excludes them from `forecasts`. Keep candidate-display policy aligned with Plan 404 D4's reviewer visibility rule.

**Pre-change.** RED: there is no publication ledger or selection invariant; Plan 328 retry can supersede a forecast row without a human publication decision.

### T2b — Finish backup proof for every pending publication

**Outcome.** Every forecast published with `preservation_at_publish = backup_pending` receives its own verifiable Plan 340 attestation within the configured window, even if it was later replaced or withdrawn; otherwise an overdue backlog closes new CHWRR publication writes and alerts operations.

**In / Out.** In: extend Plan 340 T2's sample-only host backup/restore path to enumerate **all** pending published forecast IDs represented by a transactionally consistent database dump. Record their IDs and expected evidence/output/image hashes in the protected manifest; on a clean restored database, verify each chain and retained runtime image, then append an attestation for each eligible ID. An explicit set-proof scheme is acceptable only if it proves every member individually and supports per-forecast assessment. A publication committed after the dump cutoff remains pending for the next run; never attest it from the earlier dump. Make processing restartable and idempotent, reconcile attestations lost when a dump is restored, and report backlog count, oldest pending age, per-ID failure and next retry. Keep the existing representative backup health proof distinct from this all-published-IDs obligation. The host publishes both global health/backlog and per-forecast verified-proof assessments (attestation ID, backup ID, hashes, check time) into a freshness-bounded DB projection using a dedicated credential with only the needed projection privileges, never the general forecast worker credential. The API derives review/publish eligibility from these trusted records; `sapphire_api` reads but cannot set health or proof state. A stale or missing projection fails closed even if an old attestation row exists. The protected volume and daily host schedule remain Plan 340/DHM deployment inputs. Out: Plan 344 cold archive, assuming one sample attestation covers all forecasts, or blocking an on-time alert-day publication while its own proof is still pending.

**Verification.** `uv run pytest tests/unit/ops/test_evidence_backup_host.py tests/integration/ops/test_protected_evidence_restore.py tests/integration/store/test_forecast_publication_store.py`; publish several forecasts across stations/models after the last dump, replace or withdraw one before the next backup, back up and restore them together, assert a distinct valid attestation or honest set membership for **every previously published ID**, then assess each as complete. A forecast published after the dump cutoff remains pending; one corrupt/missing snapshot or image cannot silently skip an ID or mark its proof complete. Cover a failed run, restart and idempotent retry, multiple backup generations, restored-dump attestation reconciliation, a backlog older than the configured window blocking new publish writes and raising an operations health signal, and recovery after the backlog clears. Exercise `docker/bootstrap-roles.sql` grants: API/forecast worker cannot forge healthy state or write attestations outside their grants; the host health identity cannot publish forecasts. Measure representative six-station backup/restore duration and capacity before enabling the pending path.

**Pre-change.** Plan 340 T2 verifies and attests only one eligible sample forecast per backup, so it cannot satisfy per-publication pending-proof deadlines.

### T3 — Review writes and publication reads

**Outcome.** The dashboard can inspect candidates, publish a new or replacement forecast, or withdraw a current selection; activated CHWRR consumer reads use human publication selections and retain earlier publications.

**In / Out.** In: review and consumer API routes/schemas, store queries, generated OpenAPI, cache invalidation, all forecast-capable API/legacy JSON/forecast-lab/export surfaces, dashboard handoff contract and tenant-scoped activation switch. If Plan 402 has already landed, update its `docs/spec/api-v1-map.openapi.json` and drift test for shared forecast routes. If 341 lands first, document the forecast contract in `docs/spec/types-and-protocols.md` and generated OpenAPI; Plan 402 T4 then creates its map contract from the current schema. Do not create a second map contract. Out: `sapphire-flow-map` repository, bulk publish, public anonymous feed. Every activated CHWRR PRINCIPAL forecast route, including legacy `data.json` and forecast-lab if offered to consumers or reviewers, uses the publication selection or historical ledger rather than `forecasts.status = 'published'`; a reviewer token cannot turn an ordinary route into a RAW view. Explicit human review routes retain RAW access to unpublished CHWRR candidates, and separately authorized internal diagnostics may retain RAW access for other tenants. Plan 402's two REVIEW routes have no forecast values and remain outside this gate. Plan 401 is therefore not a prerequisite for T1/T2/T3's human review/write path. Current list/latest exclude replaced IDs, while historical detail serves replaced IDs with original values. Historical CHWRR consumer responses include withdrawn tombstones but never withdrawn values.

**QC disclosure.** Plan 402's `qc_flags[].detail` can contain forecast values and observation-derived baseline statistics. Apply the same publication gate to this field on ordinary forecast routes; strip it from unpublished-ID errors and metadata-only withdrawal tombstones. Test both summary and detail responses so a hidden value cannot leak through a QC flag.

**Plan 404 route.** If its rejected-forecast route is already present, wire D4's gate predicate during this plan's activation, add the named station-granted human `review` branch for full diagnostic access, and add the route to the inventory. If Plan 404 lands later, its T3 adds that REVIEW route and wires the same predicate and human branch. In either order, verify gated reviewer metadata-only responses, full station-granted human and admin diagnostic responses, revoked/out-of-scope human and consumer denial, and rejection of a rejected-record ID on every forecast publication path.

**Verification.** `uv run pytest tests/unit/api/test_forecast_publication_api.py tests/unit/api/test_api_forecasts.py tests/unit/api/test_api_stations.py tests/unit/api/test_forecast_lab.py`; integration test with two models at one issue time, a Plan 328 automatic retry, human same-key replacement, older issue time, reasoned withdrawal of both selected and historical IDs, station-scoped consumer versus unscoped admin, bounded historical/change-feed pagination with same-timestamp concurrent inserts and retry/resume, selected-forecast publication event/retry, and a route inventory that cannot disclose unpublished or withdrawn values to an activated CHWRR consumer on ordinary routes while internal diagnostics still work under explicit authorization. Extend the existing exhaustive route-auth matrix with a human-only review class and negative service-token checks; if Plan 401 has landed, extend its REVIEW-aware matrix and add reviewer negatives. Test review GET and POST with a named, station-granted human; deny every currently supported service-token role, revoked humans, and out-of-scope humans. After Plan 401 lands, test same-scope reviewer/consumer response parity on ordinary forecast list/detail/latest, publication history and change feed, including unpublished IDs, replaced values and withdrawal tombstones. If Plan 402's QC-rules and skill routes exist, classify them as REVIEW diagnostics with no forecast-value publication filter; otherwise Plan 402 T4 must record this classification when it lands. Review responses show evidence and backup-pending reasons; the API blocks absent evidence and unhealthy protected backup even if the dashboard enables its button. Prove current/list reads still serve a selected source whose forecast row became `superseded`, then switch only after the human replacement event; by-ID/history can still read the replaced forecast's original values until an exceptional reasoned retraction tombstones that ID. In staging, prove consumer access is blocked before a pre-gate image is run during rollback.

**Pre-change.** RED: existing GET routes expose RAW forecasts; publish/withdraw/latest-published routes return 404 or 405.

### T4 — Documentation and operations handoff

**Outcome.** CHWRR and the other dashboard implementer have an exact workflow and API contract, including the distinction between automatic forecast supersession, historical publication replacement and reasoned withdrawal.

**In / Out.** In: `docs/architecture-context.md`, `docs/spec/types-and-protocols.md`, `docs/standards/security.md`, `docs/standards/logging.md`, `docs/standards/cicd.md` activation/rollback runbook, API docs and affected handover text. Reconcile the older Flow 3 `forecasts.status -> published` sketch with the additive CHWRR publication ledger and its history semantics. Document the trusted host-backup-health projection, forecast-specific `backup_pending` clock, overdue operations alert and fail-closed publication gate. Document the Plan 401 reviewer-token parity on ordinary published-only reads, the named-human candidate/write boundary, and Plan 402 REVIEW route classification. Keep DRAFT Plan 342's replacement event and linked-warning joint-transaction contract aligned with withdrawal. Out: UI implementation or a claim that bulletin publication exists.

**Verification.** Bounded inspection of generated OpenAPI against the documented contract; `uv run pytest tests/unit/api/test_forecast_publication_api.py`.

**Pre-change.** N/A — documentation/handoff task; T1–T3 provide behavioral RED evidence.

## Exit gates

The task checks pass, including the pre-Plan-342 fail-closed linked-warning tests; migration and role tests cover a real PostgreSQL schema and the prior image's read compatibility; the changed-module Ruff and pyright gates in `docs/workflow.md` pass. Plan 342 T2 verifies the joint forecast/warning transaction after 341 is complete; it is not a 341 exit gate. Before merge, the full suite passes after the final code change. Prove that Plan 328 automatic retry cannot change publication selection, that human same-key replacement retains older published values without a withdrawal, and that an exceptional historical retraction only tombstones its own ID. Block publication of pre-evidence or materially incomplete forecasts; allow the owner-approved forecast-specific `backup_pending` state only behind a healthy protected-backup gate. T2b must prove every pending published ID, the overdue-proof operations alert and fail-closed future writes; Plan 340's one-sample rehearsal is insufficient. The IdP deployment inputs, MFA guarantee and dashboard origin must be confirmed before enabling writes outside a test IdP. Keep CHWRR withdrawal or same-key replacement of a selection with a linked current warning disabled until Plan 342's joint transaction exists. Enable CHWRR publish writes and consumer publication mode only after T2b, Plan 342's selected-forecast recheck and warning-outlet gates, and Plan 340's protected backup/restore and no-cleanup policy cover publication history; keep the switches off on the Mac mini and in Swiss/internal deployments.

## Dependency graph

```json
{
  "phases": [
    {"id": "phase-1", "tasks": ["T1"]},
    {"id": "phase-2", "tasks": ["T2"], "depends_on": ["phase-1"]},
    {"id": "phase-3", "tasks": ["T2b"], "depends_on": ["phase-2"]},
    {"id": "phase-4", "tasks": ["T3"], "depends_on": ["phase-3"]},
    {"id": "phase-5", "tasks": ["T4"], "depends_on": ["phase-4"]}
  ]
}
```
