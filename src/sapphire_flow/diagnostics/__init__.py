"""T1-only diagnostics — never wired into operational assembly.

Modules here are invoked by manual staging checks (e.g. Plan 129's T1) against
raw provenance-bearing rows. They are NOT production safeguards.
"""
